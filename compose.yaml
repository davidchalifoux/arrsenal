services:
  arrsenal:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - arrsenal-config:/config
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  arrsenal-config:
