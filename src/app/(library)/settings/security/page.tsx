import { SecuritySettings } from "@/components/security-settings";

export const metadata = { title: "Security | Arrsenal" };

export default function SecurityPage() {
  return <SecuritySettings />;
}
